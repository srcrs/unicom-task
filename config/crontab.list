#40 9,20 * * * cd ${SCRIPT_DIR} && cat /config/config.json > config.json && python main.py
40 8,20 * * * cd ${SCRIPT_DIR} && cat /config/config.json > config.json && python index.py
0 23 * * * cd ${SCRIPT_DIR} && git fetch --all && git reset --hard origin/${SCRIPT_BRANCH}
